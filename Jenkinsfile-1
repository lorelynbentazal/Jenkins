pipeline {
  agent any
    stages {
      stage('Scan') {
        steps {
           withSonarQubeEnv('sonar') {
             sh "${Scanner_Home}/bin/sonar-scanner"
             }
            }
           }
  }
}

